# install python
https://python.org

# install virtualenv
pip install virtualenv

# activate virtualenv
source bin/activate

# install deps
pip install -r requirements.txt

# start up server 
python index.py
